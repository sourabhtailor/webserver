pipeline {
     agent any
     stages {
         stage('show the code') {
           steps {
               sh 'ls -al'
               sh 'cat jenkinsfile'
     }
   }
         stage('building the code') {
           steps {
             echo "now code is building"
    }
  }
}
}
